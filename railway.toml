[build]
builder = "nixpacks"
buildCommand = "DISABLE_ADMIN=false yarn workspace @dtc/backend build"

[deploy]
startCommand = "yarn workspace @dtc/backend run start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 300
